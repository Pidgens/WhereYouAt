apply plugin: 'com.android.application'
android {
    signingConfigs {
        config {
            keyAlias 'calc'
            keyPassword 'doge1234'
        }
    }
    compileSdkVersion 17
    buildToolsVersion "22.0.1"
    defaultConfig {
        applicationId "com.codeherenow.sicalculator"
        minSdkVersion 8
        targetSdkVersion 17
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
}